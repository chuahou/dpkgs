#!/usr/bin/env -S make -f

%:
	dh $@

override_dh_auto_clean:
	# We perform build before debuild, hence we should not clean

override_dh_auto_build:
	# We perform build before debuild, hence there is no need to call make again

override_dh_auto_install:
	dh_auto_install -- PREFIX=/usr
